<%= erb :project_navigation %>

<table class="table">
  <thead>
    <tr>
      <th>Channel</th>
      <th>Release</th>
      <% @project.channels.map(&:current_build).uniq.sort.reverse.each do |build| %>
        <th style="text-align:center">Build <%= build %></th>
      <% end %>
    </tr>
  </thead>
  <tbody>
    <% @project.channels.each do |channel| %>
      <tr>
        <th width="10"><a href="<%= channel_path(channel) %>"><%= channel %></a></th>

        <td width="10"><a href="<%= release_path(channel.current_release) %>"><%= channel.current_release %></a></td>

        <% @project.channels.map(&:current_build).uniq.sort.reverse.each do |build| %>
          <td align="center" class="<%= 'success' if channel.current_build == build %>">
            <% if channel.current_build == build %>
              <span class="glyphicon glyphicon-ok text-success"></span>
            <% end %>
          </td>
        <% end %>
      </tr>
    <% end %>
  </tbody>
</table>
