<%= erb :project_navigation %>

<div class="panel panel-default">
  <% if @processes.any? %>
    <div class="table-responsive">
      <table class="table">
        <thead>
          <tr>
            <th>Name</th>
            <th>Command</th>
          </tr>
        </thead>
        <tbody>
          <% @processes.each do |process| %>
            <tr>
              <th><nobr><code><%= process.name %></code></nobr></th>
              <td width="100%"><code><%= process.command %></code></td>
            </tr>
          <% end %>
        </tbody>
      </table>
    </div>
  <% else %>
    <div class="panel-body">
      <p>No process roles defined in the <code>Procfile</code> in the <code><%= @pipeline.branch %></code> branch of this project. Add this file in order to track process roles.</p>
    </div>
  <% end %>
</div>
